{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": [
    "OrionSmithers"
  ],
  "relatedServers": [
    "pay-mcp",
    "hyperd-mcp",
    "Lucy — AI Agent for Hire",
    "WebLens MCP Server",
    "BlueAgent x402 Services",
    "x402 MCP Payment"
  ],
  "name": "Agent Data Pro",
  "description": "Premium research articles for AI agents with x402 payment on Base network.",
  "version": "1.0.0",
  "type": "mcp",
  "capabilities": {
    "tools": true,
    "resources": false,
    "prompts": false
  },
  "tools": [
    {
      "name": "get_articles",
      "description": "List all available articles with prices and previews",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "get_article",
      "description": "Get full article content - requires x402 payment",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "The unique identifier of the article to retrieve (e.g., 'on-chain-trading-signals')"
          }
        },
        "required": ["id"]
      }
    },
    {
      "name": "get_crypto_price",
      "description": "Get the current price of a cryptocurrency (BTC, ETH, SOL, etc.)",
      "inputSchema": {
        "type": "object",
        "properties": {
          "symbol": {
            "type": "string",
            "description": "The cryptocurrency symbol (e.g., BTC, ETH, SOL, DOGE)"
          }
        },
        "required": ["symbol"]
      }
    }
  ],
  "payment": {
    "protocol": "x402",
    "currency": "USDC",
    "network": "base"
  }
}
