{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": [
    "OrionSmithers"
  ],
  "name": "Agent Data Pro",
  "description": "Premium research articles for AI agents with x402 payment on Base network.",
  "version": "1.0.0",
  "type": "mcp",
  "capabilities": {
    "tools": true,
    "resources": false,
    "prompts": false
  },
  "tools": [
    {
      "name": "list_articles",
      "description": "List all available premium research articles with prices and previews. Free to call — no payment required.",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "get_article",
      "description": "Retrieve full article content (requires x402 payment). Call list_articles first to get valid article IDs.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "The article ID to retrieve (e.g., 'on-chain-trading-signals', 'defi-vulnerabilities')."
          }
        },
        "required": ["id"]
      }
    },
    {
      "name": "get_crypto_price",
      "description": "Fetch current cryptocurrency price data including price, 24h change, market cap, and volume.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "symbol": {
            "type": "string",
            "description": "Cryptocurrency symbol (e.g., BTC, ETH, SOL)."
          }
        },
        "required": ["symbol"]
      }
    }
  ],
  "payment": {
    "protocol": "x402",
    "currency": "USDC",
    "network": "base"
  }
}
